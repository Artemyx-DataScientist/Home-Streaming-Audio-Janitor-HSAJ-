# Общие настройки для hsaj-bridge.service и hsaj-core.service.
# Скопируйте этот файл в /etc/hsaj/hsaj.env и отредактируйте под свою среду.

# Корневая директория проекта (репозиторий или установленный билд).
HSAJ_ROOT=/opt/hsaj

# Путь до конфига ядра. Если не задан, hsaj ищет configs/hsaj.yaml в HSAJ_ROOT.
HSAJ_CONFIG=/etc/hsaj/hsaj.yaml

# Добавьте bin виртуального окружения Python, чтобы был доступен cli hsaj.
PATH=/opt/hsaj/core/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin

# Настройки bridge.
BRIDGE_PORT=8080
BRIDGE_WS_PATH=/events
HSAJ_BRIDGE_WS=ws://localhost:8080/events
# Уберите комментарий, если хотите отключить тестовый фид треков в bridge.
# BRIDGE_DISABLE_DEMO=1
