/**
 * Стартует dev-экземпляр bridge и выводит диагностический лог.
 * @returns {void}
 */
const startBridge = () => {
  console.log("HSAJ bridge dev server started. Waiting for external event sources...");
};

startBridge();
